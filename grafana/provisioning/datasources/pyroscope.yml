apiVersion: 1

datasources:
  - name: Pyroscope
    type: grafana-pyroscope-datasource
    uid: pyroscope
    access: proxy
    url: http://pyroscope:4040
    isDefault: false
    jsonData:
      # The Pyroscope datasource auto-detects profile types from the metrics
      # the Pyroscope server returns (process_cpu, etc.).
